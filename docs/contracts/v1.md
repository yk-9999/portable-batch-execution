# Contract v1

Pydantic v2 frozen, extra-forbid models define durable input manifests, jobs,
shards, waves, attempt records, and immutable manifest revisions. Operations and
security profiles are closed identifiers; executable text and secret-bearing
artifact URIs are rejected.
