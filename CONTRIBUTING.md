# Contributing

Install [uv](https://docs.astral.sh/uv/) and run:

```bash
uv sync --dev
uv run ruff check .
uv run pytest
```

Before opening a pull request, run the lint and test commands above and keep
changes focused on the issue being addressed.
